services:
  app:
    container_name: gerardoortiz_web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - ${HOST_PORT}:3000
    environment:
      - HOSTNAME=${HOSTNAME}
      - PORT=3000
